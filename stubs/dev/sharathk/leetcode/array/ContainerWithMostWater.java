package dev.sharathk.leetcode.array;

public class ContainerWithMostWater {
    /**
     * LeetCode 11. Container With Most Water
     *
     * <p>Given {@code n} non-negative integers {@code height[0..n-1]}, where each represents
     * a vertical line at position {@code i} of height {@code height[i]}, finds two lines that,
     * together with the x-axis, form a container holding the most water, and returns the
     * maximum amount of water that container can hold.
     *
     * <p>Example: {@code maxArea(new int[]{1, 8, 6, 2, 5, 4, 8, 3, 7})} returns {@code 49},
     * from the lines at indices {@code 1} and {@code 8} (heights {@code 8} and {@code 7}),
     * spanning a width of {@code 7}.
     *
     * @param height the heights of the vertical lines, containing at least two elements
     * @return the maximum amount of water a container formed by two of the lines can hold
     */
    public static int maxArea(int[] height) {
        // TODO: implement
        throw new UnsupportedOperationException();
    }
}
