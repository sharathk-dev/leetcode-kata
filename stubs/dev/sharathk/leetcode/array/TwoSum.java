package dev.sharathk.leetcode.array;

public class TwoSum {
    /**
     * LeetCode 1. Two Sum
     *
     * <p>Returns the indices of the two numbers in {@code nums} that add up to {@code target}.
     *
     * <p>Example: {@code twoSum(new int[]{2, 7, 11, 15}, 9)} returns {@code [0, 1]},
     * since {@code nums[0] + nums[1] == 9}.
     *
     * @param nums   the array of integers to search
     * @param target the target sum
     * @return an array of the two indices whose values sum to {@code target},
     *         or an empty array if no such pair exists
     */
    public static int[] twoSum(int[] nums, int target) {
        // TODO: implement
        throw new UnsupportedOperationException();
    }
}
