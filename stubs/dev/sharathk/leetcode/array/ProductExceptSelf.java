package dev.sharathk.leetcode.array;

public class ProductExceptSelf {
    /**
     * LeetCode 238. Product of Array Except Self
     *
     * <p>Returns an array {@code answer} such that {@code answer[i]} is the product of all
     * elements of {@code nums} except {@code nums[i]}, without using division and in
     * O(n) time.
     *
     * <p>Example: {@code productExceptSelf(new int[]{1, 2, 3, 4})} returns
     * {@code [24, 12, 8, 6]}.
     *
     * @param nums the array of integers
     * @return an array where each element is the product of all other elements in {@code nums}
     */
    public static int[] productExceptSelf(int[] nums) {
        // TODO: implement
        throw new UnsupportedOperationException();
    }
}
