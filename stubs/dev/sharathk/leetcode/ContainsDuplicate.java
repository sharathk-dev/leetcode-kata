package dev.sharathk.leetcode;

public class ContainsDuplicate {
    /**
     * LeetCode 217. Contains Duplicate
     *
     * <p>Returns whether any value in {@code nums} appears at least twice.
     *
     * <p>Example: {@code containsDuplicate(new int[]{1, 2, 3, 1})} returns {@code true},
     * since {@code 1} appears twice.
     *
     * @param nums the array of integers to check
     * @return {@code true} if any value appears at least twice, {@code false} if every
     *         element is distinct
     */
    public static boolean containsDuplicate(int[] nums) {
        // TODO: implement
        throw new UnsupportedOperationException();
    }
}
