# leetcode-kata

Java + JUnit practice repo. Each problem has a stub under `src/main/java` with tests under `src/test/java`.

Solutions are intentionally not committed — implement the stub, run the tests, then reset to try again:

```
./gradlew test
scripts/reset.sh --all
```
